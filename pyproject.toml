[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hisi-interface"
version = "0.1.0"
description = "Real-time Automatic Speech Recognition (ASR) interface with modular architecture"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Diabolocom Researchers"}
]
keywords = ["asr", "speech-recognition", "realtime", "whisper", "webrtc"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic>=2.11.0",
    "fastrtc>=0.0.25",
    "librosa>=0.11.0",
    "numpy>=2.2.0,<2.3.0",
    "soundfile>=0.13.0",
    "whisper>=1.1.10",
    "mlx-whisper>=0.4.0", # Optional, for Apple Silicon
    "typer>=0.15.0",
    "rich>=14.0.0",
    "jiwer>=3.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
    "python-multipart>=0.0.20",
    "aiofiles>=24.1.0",
    "openai-whisper>=20250625",
    "av>=15.1.0",
]

[project.optional-dependencies]
# MLX Whisper is only available for macOS (Apple Silicon)
mlx-whisper = [
    "mlx-whisper>=0.4.0; sys_platform == \"darwin\""
]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "black>=23.0.0",
    "isort>=5.12.0",
    "mypy>=1.5.0",
    "pre-commit>=3.4.0",
    "ruff>=0.11.0",
]


[project.urls]
Homepage = "https://github.com/Diabolocom-Research/HISI-interface"
Repository = "https://github.com/Diabolocom-Research/HISI-interface"

[project.scripts]
hisi-interface = "asr_interface.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["asr_interface"]

[tool.black]
line-length = 88
target-version = ['py310']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 88
known_first_party = ["asr_interface"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=asr_interface",
    "--cov-report=term-missing",
    "--cov-report=html",
]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["asr_interface"]
omit = [
    "*/tests/*",
    "*/test_*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/env/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]
 